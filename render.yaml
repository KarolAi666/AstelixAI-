
services:
  - type: web
    name: AstelixAI-
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py --host 0.0.0.0 --port 10000
    envVars:
      - key: PORT
        value: 10000
      - key: HOST
        value: 0.0.0.0
    plan: free
